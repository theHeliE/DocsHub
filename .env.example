# Copy this file to .env in the directory you launch the client from, then edit.
# .env itself is gitignored, so your local setting stays out of the repo.

# Host and port of the collaboration server.
# The client derives ws://<host>/ws and http://<host>/document/ from this.
DOCSHUB_SERVER=localhost:8080
